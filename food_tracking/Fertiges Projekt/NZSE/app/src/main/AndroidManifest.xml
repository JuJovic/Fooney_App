<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fooney">
        <application
            android:allowBackup="true"
            android:icon="@mipmap/fooney"
            android:label="FooNey"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
            <activity android:name=".RecipeDetailsActivity"></activity>
            <activity android:name=".MainActivity" />
            <activity android:name=".JJMainActivity" />
            <activity android:name=".add_product" />
            <activity android:name=".get_product" />
            <activity android:name=".bestand_recyclerview" />
            <activity android:name=".viewholder_product" />
            <activity android:name=".Homescreen">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>
        </application>

    </manifest>